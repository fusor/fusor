<div class="row">
  <div class="col-md-12">
    <br />
    <form class="form form-horizontal">
      {{text-f label="Root Password" type="password" value=rhevRootPassword cssId="rhev-root-password"
      isRequired=true disabled=isStarted validator=passwordValidator
      help-inline="Applies to root user accounts for deployed RHV hosts"
      placeholder="Must be 8 or more characters"}}

      {{text-f label="Confirm Root Password" type="password" value=confirmRhevRootPassword cssId="confirm-rhev-root-password"
      isRequired=true disabled=isStarted validator=confirmRhevRootPasswordValidator
      placeholder="Must match root password"}}

      {{text-f label="Engine Admin Password" type="password" value=rhevEngineAdminPassword cssId="rhev-engine-admin-password"
      isRequired=true disabled=isStarted validator=passwordValidator
      help-inline="Applies to admin user account for RHV web UI"
      placeholder="Must be 8 or more characters"}}

      {{text-f label="Confirm Engine Admin Password" type="password" value=confirmRhevEngineAdminPassword cssId="confirm-rhev-engine-pdmin-password"
      isRequired=true disabled=isStarted validator=confirmRhevEngineAdminPasswordValidator
      placeholder="Must match engine admin password"}}

      {{text-f label="Data Center Name" value=rhevDataCenterName placeholder="Leave blank for default" cssId="rhev-data-center-name"
               disabled=isDCConfigDisabled validator=dataCenterNameValidator showValidationError=true}}

      {{text-f label="Cluster Name" value=rhevClusterName placeholder="Leave blank for default" cssId="rhev-cluster-name"
               disabled=isDCConfigDisabled validator=clusterNameValidator showValidationError=true}}

      <span data-qci="rhev-cpu-type">
        {{select-simple-f label="CPU Type"
                          content=cpuTypes
                          value=rhevCpuType
                          prompt=defaultCpuFamily
                          renderInPlace=true
                          disabled=isStarted
                          action="setSelectValue"
                          fieldName="rhevCpuType"}}
      </span>
    </form>
  </div>
</div>

{{cancel-back-next backRouteName=optionsBackRouteName
                   disableBack=false
                   nextRouteName="storage"
                   disableNext=disableNextRhevOptions
                   disableCancel=isStarted
                   deploymentName=deploymentName}}

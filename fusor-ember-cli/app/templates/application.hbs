{{#if isEmberCliMode}}
  {{#if isLoggedIn}}
    {{partial "topbar"}}
  {{/if}}

  {{#if showMainMenu}}
    {{partial "mainmenu"}}
  {{/if}}
{{/if}}

{{!upstream-downstream isUpstream=isUpstream}}

{{!render 'side-menu'}}

<div {{bind-attr class="isContainer:container:container-fluid"}}>
  {{outlet}}
</div>

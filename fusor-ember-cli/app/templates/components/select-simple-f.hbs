{{#base-f label=label labelSize=labelSize inputSize=inputSize unitsSize=unitsSize unitsLabel=unitsLabel isRequired=isRequired helpText=helpText}}

  {{#power-select options=content
                  selected=value
                  placeholder=prompt
                  disabled=disabled
                  data-qci=cssId
                  renderInPlace=renderInPlace
                  onchange=(action "setValue") as |name|}}
    {{name}}
  {{/power-select}}

  {{yield}}

{{/base-f}}

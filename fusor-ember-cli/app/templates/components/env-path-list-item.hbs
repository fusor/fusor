<label class="{{bgColor}}">
  <span id="{{envCssId}}" class="{{if isNonSelectableLibrary 'ident-library-label'}}">
    {{#if isSelectable}}
      {{radio-button value=env.name groupValue=selectedEnvironment.name disabled=disabled dataQci=dataQci}}
    {{/if}}
    {{env.name}}
  </span>
</label>

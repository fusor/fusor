import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('deployment-role', 'Unit | Component | deployment role', {
  // Specify the other units that are required for this test
  // needs: ['component:foo', 'helper:bar'],
  unit: true
});

import {
  rawText
} from 'fusor-ember-cli/helpers/raw-text';

module('RawTextHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = rawText(42);
  ok(result);
});

import { hostType } from '../../../helpers/host-type';
import { module, test } from 'qunit';

module('Unit | Helper | host type');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = hostType(42);
  assert.ok(result);
});
